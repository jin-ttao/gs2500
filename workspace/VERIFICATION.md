# 로컬 워크플로 검증

## 최신: 적용한 진열 제안의 성과 추적 (2026-09-22)

- 메뉴를 담당 점포 실적 → 진열 시뮬레이션 → 진열 시뮬레이션 결과 → 점포 운영 순서로 변경했다. 기존 review 경로를 유지하며 검색에도 새 이름을 연결했다.
- 적용 후 14일·28일이 지난 합성 제안 4건을 구성했다. 각 건은 같은 길이의 적용 전·후 기간, 점포 전체 매출, 주차별 수치, 품절 시간, 적용 이슈와 후속 조치를 갖는다. 30일·24 SKU의 과거 모형 예상은 범위가 다른 수치로 별도 표기했다.
- 미래 시점 가이드·미승인 매대 목록을 결과 화면에서 제거했다. 이번 세션의 신규 승인·점주 회신은 실제 경과 성과로 만들지 않고 별도 펼침 영역에 유지한다.
- 역삼중앙점 재고 우선안의 추천 제목을 시의성·온라인 관심·유사 점포·과거 판매의 데모 가정으로 구체화했다. 변경할 선반 칸은 진한 초록 테두리와 이동 배지로 강조했다.
- workspace 자동 검사 **221/221 통과**. 마지막 문구 정합성 수정 후 이력 모델·뷰 **8/8** 재검사, 전체 빌드·구문·diff 검사 통과. 기존 브라우저 E2E의 변경된 화면 선택자와 필터 검사를 갱신했으며 해당 E2E 스크립트 전체를 실행한 것은 아니다.
- 실제 브라우저에서 메뉴 순서, 4건 → 2주 2건 → 재점검 1건, 4주+재점검의 빈 결과 → 4주 2건 → 전체 4건을 확인했다. 이슈·메모 펼침과 적용안 이동/뒤로 가기 시 필터·펼침 복원을 확인했다. 390px 화면에서 본문 scrollWidth/clientWidth 모두 390px, 각 카드 348px로 가로 넘침이 없었다. 임시 화면 크기를 복원했다.

## 최신: 전광판 상단 간소화 (2026-09-22)

- 사용자 지정 영역의 준비 완료 박스, 반복된 계산 설명, 날짜 이동 셀렉터/화살표, 이벤트 바로가기를 제거했다. 계산 중에만 짧은 진행률을 표시한다.
- 시각·재생·초기화·배속·기간 슬라이더를 간결한 툴바로 통합했다. 대표 방문·가정의 상세 기록과 합성 데이터 구분은 카드 아래에 유지한다. 계산 및 이동 속도 로직은 변경하지 않았다.
- 화면·컨트롤러·재생 관련 **50/50** 검사 통과. 빌드·문법·diff 검사 통과. 브라우저에서 불필요 영역 0개, 16개 카드 0원 초기화, 585px 웹뷰 툴바 높이 57px·가로 넘침 0 확인.

## 최신: 화면 맥락 보존·0원부터 30일 누적 재생 (2026-09-22)

- 후속 요청: 로그인 화면만 원본 콘티의 GS 블루·청록 스타일로 복원했다. 450px 웹뷰에서 로고 `#0b3c97`/`#00a4d9`, 파란 CTA, 가로 넘침 0을 실제 스크린샷으로 확인했다. 로그인 이후 Obolus 레이아웃과 데모 기능은 유지한다.
- 후속 초기화 검사: 원래 `restart()`가 다음 프레임부터 자동 재생해 첫 시간 구간이 약 60ms 뒤 합산됐다. 이제 명시적으로 pause→seek(0), 캐릭터도 0명으로 초기화한다. 브라우저에서 완료 39,516,000원→처음부터→16개 카드 0원·화면 0명·`재생 시작`을 확인하고, 시간이 지난 다음 관찰에서도 0원·진행률 0을 유지했다. 단위 검사에서 초기 매대/창고 재고 복원과 재생 버튼 이후 재진행을 확인했다.
- 후속 모션 검사: 이동 좌표는 이미 배속에 비례했으나 리깅에 기본 28배 시계를 적용해 프레임 간 발걸음이 너무 많이 진행되는 문제가 있었다. 대표 재생에만 절대 자세 시계를 도입했다. 걷기는 읽기 쉬운 표시 시간, 집기·결제는 기록된 동작 시간을 사용하며, 4배속에서 이동량·동작 시간·걷기 자세 시간이 각각 4배 진행됨을 검사했다. 실제 THREE mixer로 정지·되감기·재생성의 자세 일치도 확인했다. 기존 live 에이전트는 이 선택적 필드가 없으므로 기존 경로를 유지한다.
- 최종 자동 검사 **377/377**: workspace 209개 + demo 168개. 실제 app.js 컨트롤러 39개, world-view 컨트롤러 5개 및 대표 캐릭터 애니메이션 3개 포함. `npm run build`, `npm run check`, `git diff --check` 통과. 외부 모델 호출·배포·GitHub 변경 없음.
- 30일 계산을 Worker로 분리하고 계산 진행률·취소 경로를 추가했다. 동일 입력에 대한 원래 계산과 Worker 결과의 일치, 실제 Node background worker 실행 중 메인 타이머의 응답을 검사했다. 브라우저의 4점포 × 4안 첫 계산 표시는 **1.24초**였다. 이 수치는 이 기기의 단일 측정이며 성능 보장이 아니다.
- 브라우저에서 전체 기간 슬라이더를 시작점으로 이동하면 **16개 카드 모두 0원·0명·0건**. 12일 시작은 12일 00:00/당일 0원, 23일 시작은 23일 00:00/당일 0원으로 표시됐다. 시작일 경계의 부동소수점 반올림 오류를 수정했다.
- 4배속 실제 재생 후 약 8.87초 시점에 24일 13:00, 첫 카드 누적 31,771,700원, 화면 대표 인원 11명을 관찰했다. 다음 관찰에서 30일 완료·39,516,000원·정지 상태를 확인했다. 설계상 기본 45초/4배속 11.25초이며, 정확한 완료 시각을 브라우저 외부 시계로 연속 계측한 것은 아니다.
- 종료 시 16개 카드의 누적 매출이 각각의 별도 최종 예상과 모두 일치했다. 정지·완료 후 불변 숫자를 매 프레임 재작성하지 않도록 했고 회귀 검사했다. 독립 검사에서도 16개 안 × 30개 일자 시작·721개 시간 경계의 누계·매대/창고 재고 오류 0, 원본 기록 불변을 확인했다.
- 1280×800: 범위 필터 키보드 변경 전후 스크롤 612px 유지. 열린 일자별 상세 유지. 보드 아래 점포 B → C 후보 → 보드 복귀 시 **2,760px** 및 **12일차 일시정지** 복원. 브라우저 뒤로 가기로 C → B → 보드 이동해도 같은 위치 복원, 중복 히스토리 생성 없음. 보드는 후보 선택에 종속되지 않는 공용 위치 키를 사용한다.
- 3D 비동기 생성 이후에도 위 위치 유지. 16개 카드의 viewport/canvas 좌표·크기 불일치 0, 페이지 가로 넘침 0. 개발용 viewport override 상태에서 자동 클릭이 화면 밖 대상을 잘못 조준하는 경우 키보드 조작으로 앱 상태와 구분해 검사했다.
- 390×844: 카드 너비 322px, 16개 카드, 가로 넘침 0. 관찰 모달에서 상품 미지정 오류를 발생시켜 **모달 scrollTop 847→847**, 펼친 비교 조건·메모 보존을 확인했다. 사진 디코딩/오류 재렌더 경로는 자동 컨트롤러 테스트로 검사했다. QA 메모는 저장하지 않고 모달을 닫았다. 임시 viewport 설정 복원, 브라우저 콘솔 오류 0.
- 홈/추천 → 실제 계산·준비 결과 재진입, 매대 → 계산, 보드 ↔ 후보 선택, 승인 후 점주 화면으로 이어지는 버튼을 연결했다. 운영 추천 위치는 기록 폼에서 참고 문구로만 넘기고 실제 확인 위치로 자동 채우지 않는다.

이 재생은 **로컬 합성 모형의 시간별 누계 + 기록된 대표 방문의 압축 동선**이다. 실측 POS, 모든 방문객의 연속 30일 행동, 정확한 개별 결제 순간, JEV 판단, 사진 자동 학습을 구현했다고 주장하지 않는다. 배속·탐색·반복 재생은 구매·재고 계산을 다시 실행하거나 최종 수치를 바꾸지 않는다.

## 최신: 팀 Obolus 레이아웃·컴포넌트 포트 (2026-09-22)

사용자가 팀 저장소 재사용을 허락한 뒤 고정 커밋의 AppLayout/AppSidebar, Memory, Dashboard, Archive/Transactions 구조와 스타일을 GS DOM 렌더러에 포팅했다. 원본 코드·글꼴 재사용과 기존 GS 구현을 `OBOLUS-PORT.md`에 구분했다. 이전 `studio.css`는 보존하지만 더 이상 로드하지 않는다. 로컬호스트만 갱신했다.

- 자동 검사: 워크플로 **167/167**, 기존 3D **165/165**, 총 **332/332 통과**. 최종 메뉴 포커스 수정 후 실제 컨트롤러 **23/23** 재검사. `npm run build`, `npm run check`, `git diff --check` 통과.
- 1280×800: 사이드바 200px, 본문 1080px, 추천 3열 각 330.7px, 커버 16:10. 사이드바 접으면 0px/본문 1280px로 변경, 가로 넘침 없음. 계정 영역은 레일 하단에 고정.
- 390×844: 추천 1열 350px, 가로 넘침 없음. 관찰 모달 x=14/너비362px/높이816px로 화면 안에서 내부 스크롤. 메뉴 열기 시 브랜드로 포커스 이동, 배경 inert 처리, 검색 열기 시 메뉴 닫힘 확인. 검사 후 viewport 기본값 복원.
- 추천 도식은 4행 × 6열이며 실제 target에 해당하는 1칸만 강조. 생수 45분 품절·2단4열 QA 기록 저장 후 해당 위치의 보충 제안이 첫 번째로 갱신됨. 다른 점포에는 직접 기록이 노출되지 않음. QA 기록은 이후 빌드 새로고침으로 초기화.
- 4점포×4안 계산 후 16개 canvas 생성, body overlay 0개. 내부 스크롤 6,242.5px 이동 후에도 16개 카드의 viewport/canvas 좌표와 크기 불일치 0개. 신규 콘솔 오류 없음.
- 역삼중앙점 B안 승인(+1.7% 합성 계산) 후 점주에게 해당 안만 표시됨. 점주 전환 뒤 브라우저 뒤로 가기로 관리자 후보에 접근하던 경로를 막고 실제 뒤로 가기 및 자동 회귀 검사로 확인. 점주 화면의 후보 선택 버튼 0개.
- 원본 폰트 3개는 로컬 제공하며 공식 OFL 고지를 동봉했다. 빌드 산출물의 Gateway/OpenRouter 비밀키 패턴 미검출. JEV·POS·지갑·결제·배포 연결은 추가하지 않았다.

## 이전: 운영 UX·근거 기반 추천 루프 (2026-09-22)

사용자 요청으로 JEV 연결을 보류하고 운영 기록 중심으로 로컬 화면을 재구성했다. Grok 스타일은 라이선스 경계를 확인한 뒤 독립 CSS로 구현했고, 사용자 Chrome에 열린 21st.dev의 차트·카드·타임라인 패턴을 참고했다. 출처와 미구현 범위는 `DESIGN-SOURCES.md`에 기록했다.

- 새 화면: 운영 개요, 운영 이슈, 다음 추천, 사진·관찰. 기존 진열 시뮬레이션·승인·점주 회신·회고는 유지했다.
- 실제 브라우저에서 참고 JPEG와 '생수 45분 품절'이라는 QA 기록을 추가했다. 사진 미리보기와 직접 관찰 1건이 나타났고 우선 추천이 샌드위치에서 생수 보충으로 바뀌었다. 합성 점포 전체 매출은 48,607,700원으로 그대로였다. 기록은 테스트 후 새로고침으로 초기화했다.
- 다른 점포로 전환하면 해당 QA 사진·직접 기록이 보이지 않았다. 28일 조회의 이전 기간 데이터가 없을 때 잘못된 증감률을 표시하지 않는다. 날씨 필터, 점포 검색, 추천 근거·단·열 표시를 클릭 확인했다.
- 상품 미지정 품절 기록 저장은 거절되고 입력한 메모가 유지됐다. 사진의 형식/크기, 디코딩 경합, 취소/초기화, HTML 이스케이프는 자동 테스트로 검사했다.
- 확정하지 않은 상품 위치는 기준안 좌표와 현장 미확인을 구분한다. 직접 전후 금액은 합성 일매출과 섞지 않는다. 기존 후보 계산과 새 운영 추천의 좌표 검증은 별개라는 문구를 표시한다.
- 최종 브라우저 입력으로 생수 2단 4열, 각 1일의 0원→1,000원 시험 기록을 저장했다. 사진·관찰 화면에 금액과 직접 확인 위치가 보존되고 비율 계산 불가를 표시했다. 다음 추천의 최우선 제안은 해당 2단 4열의 소규모 시험과 기존 참치 꼬마김밥 위치 충돌 확인으로 갱신됐다. QA 기록은 확인 후 새로고침으로 초기화했다.
- 삼성역점 B안 상세 → 승인 → 점주 화면을 클릭 확인했다. 승인 B안만 표시되고 다른 후보는 비노출이었다. 점주 화면에서는 매니저용 검색과 단축키를 노출하지 않는다. 실제 계정 권한 검증이 아닌 로컬 역할 데모다.
- 1280×720 운영 화면, 390×844 모바일 홈은 가로 넘침이 없었다. 모바일 모달의 grid 최소 너비 문제를 수정한 뒤 실제 폭 360px, 좌우 15px 여백을 확인했다. 1680×900에서 16개 카드·16개 canvas, 위치/너비/높이 불일치 0, 가로 넘침 0이었다. 임시 viewport는 기본값으로 복원했다.
- 최종 정상 화면의 브라우저 콘솔 오류 0. 빌드 산출물 442개에서 환경 파일·테스트·서버용 gateway 및 비밀키 패턴 미검출. 외부 JEV/API 호출, 배포, GitHub 변경 없음.
- 최종 `npm test`: 워크플로·계산·운영·UI **155/155**, 기존 3D **165/165**, 총 **320/320 통과**. `npm run build`, `npm run check`, `git diff --check` 통과. 모의 JEV 어댑터 테스트는 실제 호출·정확도 평가가 아니다.

### 구현의 경계

현재 추천은 사람이 확인한 기록을 누적해 규칙으로 근거·우선순위를 갱신한다. 사진 자동 인식·모델 학습·실제 POS·인과효과 추정·추천 위치를 적용한 커스텀 시뮬레이션은 구현됐다고 주장하지 않는다. 사진과 관찰은 현재 탭 메모리에서만 유지된다. 장기 학습 제품으로 전환하려면 영구 저장, SKU/위치 정정, POS 연결과 비교 실험 검증이 필요하다.

## 최신: 카드 정렬·단일 계산 기록 (2026-09-22)

사용자 스크린샷에서 카드와 3D가 따로 움직이는 현상을 수정했다. 아래 최초 구현의 '별도 하루 관찰' 설명은 이번 변경으로 대체된다.

- 원인: body의 fixed WebGL overlay와 카드 DOM이 별도로 스크롤·클리핑됐다. 기존 `createCardWorlds`의 기본 호환 동작을 보존하면서 `presentation: embedded` 옵션을 추가했다. 공유 WebGL 컨텍스트는 하나이며 카드 내부 2D canvas로 즉시 복사한다.
- 브라우저 실제 스크롤 후 첫 4개 카드의 viewport/canvas x·y·width·height 모두 동일, body overlay canvas 0개. 1680×900 크기 검사에서도 16개 카드 불일치 0, 가로 overflow 없음. 검사 후 기본 viewport로 복원했다.
- 통합 화면에서 별도 `createWorld` 실행을 제거했다. 30일 계산의 720개 시간별 원장으로 숫자·재고를 읽고, 기록된 입장 중 최대 12명/일을 재생한다. SKU 선택·상품 열·결제 금액은 같은 기록이다. 동선·체류·겹친 인원은 시각화이며 원래 동시성이나 실측 이동시간을 주장하지 않는다.
- 실제 UI에서 20일 행사 선택 → 16개 카드가 모두 20일로 전환, 행사 14–21시 가정 표시, 2배속 변경 전후 30일 수치 동일 확인. 삼성역점 B의 선택일 매출 1,184,500원과 승인용 30일 매출 39,813,800원을 구분해 표시했다.
- B 상세 → 승인 모달 → 점주 화면을 다시 클릭했다. B안 및 +0.8% 승인값 전달, 다른 후보 비노출, 신규 콘솔 오류 없음.
- `npm test`: 워크플로·계산·재생 **92/92**, 기존 3D·렌더러 **165/165**, 총 **257/257 통과**. 빌드·문법·diff 검사 통과.
- 독립 검사: 4점포×4안×30일, 6,030 재생 프레임 / 659,008 캐릭터 위치의 통행 가능·유한 방향 확인. 재생 중 원장 불변, 후보별 좌표, 실제 구매 기록에만 바구니 연결, 일자·배속·일시정지 검사 통과.

## 범위와 결정

- 2026-09-22: 사용자가 goal 프롬프트와 HTML 콘티에 맞춘 웹앱 구현을 요청했다.
- 원본 `/Users/yuchanlee/GS`에서는 별도 JEV 수정 작업이 진행 중이므로 파일 충돌을 피하기 위해 `codex/manager-workflow` worktree에서 개발했다.
- 이후 사용자가 Vercel 대신 로컬호스트를 지정했다. 공개 배포 확인은 이번 완료 범위에서 제외한다.
- 최초 구현은 30일 수요·재고 계산과 하루 3D 관찰을 분리했다. 최신 변경은 위처럼 같은 계산 기록을 재생하며 원본 독립 실행 엔진은 보존한다.
- 콘티의 표본 수·고정 상승률은 구현 한계나 계산 결과로 복제하지 않는다. 4점포/12후보는 현재 합성 fixture이며 화면은 목록을 순회한다.
- 목표 문서의 실제 문자 발송·DB·자동 사진 판독 제외 조건을 유지한다.

## 검증 방법

자동 검사: `npm test`, `npm run build`, `npm run check`, `git diff --check`.
독립 모듈 검사: 미승인 비노출, 승인 스냅샷, 다른 점포/후보 차단, 미완료 계산 거절,
부분 실행·거절·미회신, 이미지 형식/크기 오류, 예산·재고 보존, 기준선과 후보의 동일 난수 입력.

## 실제 데스크톱 브라우저 확인 (2026-09-22)

검사 URL: `http://127.0.0.1:4175/`, Codex 인앱 브라우저, 1280×720.

- 로그인 → 매대 목록 → 4점포 비교 계산 → 삼성역점 B안 → 안내문 편집·승인 → 점주 부분 실행 회신 → JPEG 접수 → 매니저 회고를 실제 클릭했다. 도구 왕복 시간을 포함한 소요 시간 **51.907초**. 사람 대상 사용성 연구의 평균 시간은 아니다.
- 승인 전 점주 화면은 제안 없음으로 표시했다. 승인 후 B안과 수정한 안내문만 표시됐고 A/C 후보는 나타나지 않았다.
- JPEG 선택 후 실제 미리보기와 `사진 접수됨`을 확인했다. Markdown 파일을 선택하면 이미지 형식 오류가 나오고 이전 사진이 유지됐다.
- 부분 실행 후 실행 어려움으로 변경하면 사진이 해제되고 거절 의견이 남았다. 초기화는 로그인 화면으로 돌아간다. 새로고침도 메모리 상태를 초기화한다.
- 회고의 실제 데모 집계는 제안 4/4, 승인 1/4, 회신 1/4, 사진 1건이었다. 별도 4주 합성 사례는 이 집계에 섞이지 않으며 미회신 사례의 관찰값은 0이 아닌 미관찰로 표시했다.
- 4점포 × 현재안/후보 3개, 총 16개의 3D 뷰포트가 렌더링됐다. 카드별 시각·매장 내 인원·결제가 진행했고 일시정지 버튼도 동작했다. 최신 정상 빌드의 콘솔 오류는 관찰되지 않았다.
- 3D 모듈에 의도적으로 HTTP 503을 반환하는 로컬 QA 프록시(4176)에서도 연결 실패 안내, 30일 계산 유지, 후보 승인, 점주 전달, 초기화가 동작했다. QA 프록시는 검사 후 종료한다.
- 브라우저가 첫 프레임에서 음수 시간 간격을 줄 수 있는 오류를 수정하고 회귀 테스트를 추가했다. 연속 사진 선택·초기화 중 디코딩 완료 시점의 경합도 방어한다.

## 빌드·서버 검사

- 최종 `npm test`: 워크플로·계산·컨트롤러 **81/81**, 기존 3D **159/159**, 총 **240/240 통과**. 컨트롤러 9개 검사는 실제 `app.js` 이벤트 핸들러를 모의 DOM에서 실행하며 브라우저 렌더링 검사와 구분한다.
- `npm run build`, `npm run check`, `git diff --check` 통과.
- 최초 구현 커밋 `a5078f7`에서는 기존 `demo/` 소스 변경이 없었다. 최신 변경은 렌더러의 선택적 embedded 모드와 관련 테스트만 추가하며 구매 판단 엔진은 변경하지 않았다.
- 루트·기존 3D·워크플로 런타임 경로는 HTTP 200. 환경 파일, 서버 파일, 테스트 파일, 경로 탈출, 미지원 API는 404.
- 빌드 산출물에서 환경/서버/세션 파일 및 비밀키 패턴 미검출. 서버는 127.0.0.1에만 바인딩한다.
- `scripts/browser-e2e.spec.mjs`는 별도 실행용 브라우저 테스트 명세다. Playwright 러너로 실행했다고 주장하지 않는다. 위 UI 검사는 실제 인앱 브라우저 조작 결과다.

## 남은 모델 한계

JEV의 실성능 검증, 실제 GS 데이터와 매출 효과, 30일간의 연속 3D 동선·재방문 기억,
진짜 점주 계정과 기기 간 전달, 사진 영구 저장은 구현·검증했다고 주장하지 않는다.
실제 네모트론 원본은 공개 합성 페르소나이며 사람의 관측 구매 기록이 아니다.
