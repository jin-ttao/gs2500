# GS2500 — 다점포 동시 진열 실험실

9개 합성 점포 × 진열 후보 4개, 총 36개 세계를 함께 실행하는 로컬 3D 데모다.
한 페이지에는 점포 4곳 × 후보 4개, 최대 16개의 실시간 3D 카드가 있다.
한 화면 높이에 모두 압축하지 않고, 카드 비율을 유지한 채 아래로 스크롤한다.
Blender 리깅 모델과 생성된 상품 포장을 사용한다. NVIDIA 공개 합성 페르소나 원본 1,000개를
로컬 데이터로 포함하고, 로컬 기준선과 선택적 JEV 평가 API를 제공한다. 실제 GS 내부 시스템은 연결하지 않는다.

## 실행

```bash
cd demo
npm ci
npm start
```

http://127.0.0.1:4173 에서 연다. 로컬 기준선에는 외부 CDN이나 API 키가 필요 없다.
`npm start`는 로컬 Node 서버(`server.mjs`)를 실행한다. Node 내장 `fetch`를 사용하므로 AI SDK는 필요 없다.
`npm test`로 데이터·공간·동시 실행·결제 동기화 테스트를 실행한다.
`npm run test:day`는 명시적인 `everyday` 그룹의 3점포 × 4안 = 12개 세계에서
점포당 잠재 고객 1,000명의 24시간을 끝까지 실행해 시계·재고 보존·결제 원장·시간별 집계를 검사한다.
검사 시간 제한을 위해 900×를 사용하며, 기본 보드의 36개 전체 세계 검사나 브라우저 속도 측정은 아니다.
Node.js 22 이상, npm, WebGL 지원 브라우저가 필요하다. Python 3은 정적 미리보기에만 사용한다.
`npm run build`는 정적 배포용 파일을 `dist`에 만들고, `npm run preview`는 이를
`http://127.0.0.1:4174`에서 확인한다. 빌드는 외부 배포를 실행하지 않는다.
정적 빌드·정적 Vercel 배포는 로컬 기준선만 실행한다. JEV는 별도 Node 백엔드가 필요하며
로컬 서버를 운영용 인증·배포 시스템으로 간주하면 안 된다. 키는 정적 파일에 포함하지 않는다.

## JEV 연결·호출 한도·현재 검증

기본 선택은 **로컬 기준선**이다. 로컬 비교·요약 결과를 JEV 결과라고 표시하지 않는다.
서버 환경변수 또는 Git에서 제외된 `demo/.env.local`에 `AI_GATEWAY_API_KEY`를 안전하게 설정하고
`npm start`로 실행한다. 실제 키를 문서·채팅·로그·브라우저에 출력하거나 커밋하지 않는다.
`.env*`는 정적 파일 허용 목록 밖이며, 서버는 `127.0.0.1` 바인딩·동일 출처 요청·입력 크기·동시성 제한을 적용한다.

사용자가 JEV를 선택하면 실시간 관찰로 전환하고 `typesafe-ai/jev`를
Vercel Gateway의 `/v1/evaluate`에 전송한다. Chat Completions나 GPT를 사용하지 않고
자동 재시도·다른 모델·로컬 판단으로의 자동 대체도 하지 않는다. 유효한 허용 행동의 선택만 적용한다.
입점(`enter/pass`)·목적지(`visit/leave`)·매대(`pick/record-unmet-demand/skip`)는 별도 판단 단계다.
상세 이야기·원본 사실·현재 미션·명시적 제약·기억·바구니·예산·시각·행사와 실제 상품 좌표를 함께 전달한다.

기본 `JEV_MAX_CALLS`는 **서버 프로세스당 20회**, `JEV_CONCURRENCY`는 최대 3이다.
설정 한도와 **현재 남은 횟수**를 혼동하지 않는다. `/api/status`의 `maxCalls` / `used` /
`callsRemaining`이 현재 프로세스 기준이며, 실패한 상류 요청도 시도 횟수를 소비한다.
1,000명×여러 판단×36개 후보 실행용 비용 한도로 설정한 값이 아니다. 한도 확대는 별도 비용 판단이 필요하다.
한도 소진·HTTP 429 등 상류 오류·비정상 응답은 실행을 중단하며 새 매출이나 대체 판단을 만들지 않는다.

네트워크를 기다리는 동안 전체 시뮬레이션 시계와 캐릭터를 멈춘다. 일시 정지 중 응답이 도착하면
결과를 보관하고 재개 전 적용·후속 호출을 하지 않는다. 초기화한 이전 실행의 늦은 응답·실패는 새 실행에 적용하지 않는다.
입력 해시·요청 ID·모델·선택·확률·응답 시간·실패 상태를 추적하고 `/api/traces`는 최근 20개 요약을 보관한다.
선택한 행동의 화면 설명은 **JEV가 생성한 자연어 추론 이유가 아니다**. 인지 여부는 공간 엔진의 가시성이고,
평가 확률도 실제 고객의 관측·보정된 구매 확률이 아니다. 반환된 비용·사용량이 없으면 미상으로 둔다.

실제 입점 평가 응답 스모크 테스트는 성공했으나 상류 HTTP 429도 관측했다.
입점→목적지→상품 선택→결제 전체 실호출 흐름은 검증 중이다. **1,000명 전체 JEV 완료 실험이나
인간 행동 정확도 검증은 아직 하지 않았다.** `npm test`의 성공은 모의 응답·불변식 검증이며 실호출 성공 횟수가 아니다.

## 합성 페르소나 원본과 파생 가정

[공식 Nemotron-Personas-Korea](https://huggingface.co/datasets/nvidia/Nemotron-Personas-Korea)의
고정 리비전에서 UUID가 서로 다른 원본 1,000개를 추출했다. 원본의 상세 이야기·지역·직업·나이 등 26필드를
그대로 보존한다. **실제 한국인이나 실제 GS 고객 1,000명의 데이터가 아니다.** CC BY 4.0 출처 표시와
리비전·추출법·무결성 해시는 [data/PERSONA-SOURCE.md](data/PERSONA-SOURCE.md)에 있다.

예산·가격 민감도·카테고리 사전값·방문 시간대 가중치·바구니 제한은 별도로 작성한 가정이며
원본 데이터에 있는 실측값처럼 표시하지 않는다. `sourceFacts` / `sourceNarratives`와 `derived`를 구분한다.
나이·성별만으로 강한 취향을 부여하지 않는다. `archetypeIndex`는 기술적 5개 버킷일 뿐 기존 5인 프로필의
직업·성격을 뜻하지 않으며 이 숫자를 바꿔도 입점·신상품 보너스가 달라지지 않는다.
개인별 가정 시간 가중치는 점포 시간곡선에 반영되고 정렬 후에도 UUID와 예약 시간이 함께 유지된다.
생수만 구매 같은 명시적 구매 목표는 유지하되, 심야 방문에 과거 템플릿의 `수업 사이`를 현재 사건으로 강제하지 않는다.
원문 이야기는 배경으로 보존한다. 이 표본은 한국 인구 또는 상권별 GS 고객을 대표하지 않는다.

## 체험

기본은 **빠른 비교**다. `하루 계산 후 비교`를 누르면 별도 Web Worker들이
원래 `world.js` 규칙으로 9점포 × 4안의 24시간을 먼저 계산한다. 준비 시간·진행률은
별도로 표시한다. 완료 후 대표 고객의 방문을 읽기 쉬운 속도로 재생한다.
같은 페이지에서 다시 보기는 저장된 메모리 기록을 재사용한다. 새로고침하면 다시 계산한다.

- 각 후보에서 하루에 걸쳐 선정한 최대 12명의 **실제 방문 경로**를 겹쳐 보여준다.
  기본 재생은 전체 약 30초이며 방문별 18~27초를 배정하고 상품 관찰·집기 장면의 화면 시간을 늘린다.
  `느리게 / 기본 / 빠르게`로 재생 배율을 0.5 / 1 / 2배로 바꿀 수 있다.
  서로 다른 시간의 방문이므로 동시 인원·혼잡·체류시간 분석에 영상 자체를 사용하면 안 된다.
  고객이나 탐색 코너, 구매, 보충 행동을 추가하지 않는다. 끝나면 반복 루프 없이 종료한다.
- 매출·수량·재고는 실제 계산의 15분 단위 체크포인트를 읽으며 금액을 선형 보간하지 않는다.
  마지막에는 원본 최종 스냅샷을 그대로 사용한다. 재생 속도·프레임 수는 결과에 영향을 주지 않는다.
- 카드의 `REPLAY · 대표 방문`, 화면 위 안내, 별도 통계 시계로 재생임을 알린다.
  보충 직원의 경로는 요약하지 않으며 보충 통계만 제공한다.
- 일시 정지·초기화는 재생에만 적용된다. 초기화로 원본 계산 결과·승인을 바꾸지 않는다.
  계산 중 초기화는 작업을 취소한다. 모드 전환은 실시간 진행 상태를 보존한다.
- 요약 JSON은 최종 통계와 모든 결제 사건을 담은 **요약 원장**이다.
  전체 노출·판단 사건은 생략했다고 명시하며 전체 원장으로 표시하지 않는다.

`실시간 관찰`로 전환하면 아래의 원래 동기화 방식으로 직접 실행할 수 있다.

1. 전체 실행을 시작하면 삼성역점(합성 오피스형), 역세권·주거지·한강변·대학가·관광지·골목·카페·공원
   데모점의 본사 표준 / 재고 우선 / 균형 후보 / 신상품 탐색안, 총 36개가 함께 진행된다.
2. **각 카드 자체가 실시간 3D 시뮬레이션 화면**이다. 각 점포의 후보 4개는 2×2로 배치한다.
   카드의 가로·세로 비율은 약 390:412, 3D 영역은 약 7:4를 유지해 가로로 납작해지지 않는다.
   점포 묶음은 화면 폭에 따라 1~2열이고, 남은 카드는 세로 스크롤로 확인한다.
   전체 점포는 4곳 / 4곳 / 1곳의 세 페이지다. 터치·트랙패드의 좌우 스와이프,
   마우스 드래그, 이전·다음 버튼과 페이지 점으로 이동하며, 페이지 이동은 실험을 초기화하지 않는다.
   모바일에서는 후보 4개의 2×2 배치를 유지하고 같은 페이지의 점포 묶음을 세로로 쌓는다.
   각 점포·후보의 실제 고객이
   걷고, 상품을 살펴보고, 집고, 결제한다. 아래 지표는 그 세계의 결제와 퇴장 결과다.
   카드를 누르면 상세 3D 관찰 창이 열리고, 닫기나 Escape로 비교 보드에 돌아온다.
   관찰 창을 여닫아도 다른 실험은 계속 진행되며 초기화하지 않는다.
3. 하루는 00:00–24:00이다. 점포당 잠재 고객 1,000명이 시간대별로 지나가며
   필요·영업 시간·혼잡에 따라 입장하거나 지나친다. 전원 강제 방문이 아니다.
   UI의 1 / 4 / 16 / 32 / 128 / 900배속은 입장·이동·탐색·집기·결제·보충·퇴장에
   함께 적용된다. 기본 32배속이며 더 자세한 동작은 1~4배속으로 관찰한다. 카드 모션만 따로 늦추지 않는다.
4. 일시 정지는 36개 세계와 지표·캐릭터 포즈를 함께 멈춘다. 카메라 조작은 가능하다.
   탭이 백그라운드로 전환되어도 정지하며, 복귀 후 재개 버튼으로 이어서 실행한다.
5. 초기화는 모든 방문·재고·결과를 초기 상태로 돌린다. 선택한 후보와 배율은 유지된다.

## 실시간 관찰의 동기화 구조

기존 UI의 독립적인 `createExperiment / visit` 배치 실행을 제거했다.

- `lab.js` 한 곳만 시간을 전진시킨다. 0.05초 고정 간격마다 미완료 후보 36개를
  모두 업데이트한 다음 브라우저에 렌더링 시간을 돌려준다. 모든 점포가 비어 있고
  보충 작업도 없으면 다음 실제 예약 사건까지만 함께 건너뛴다. 행동은 생략하지 않는다.
- `world.js`가 실제 이동·상품 비교·집기·매대 차감·대기열·결제·퇴장과
  점주의 창고→매대 보충 작업을 계산한다.
- `store.js`는 선택된 **같은 world 객체**의 위치·상태·재고를 그린다.
  자체 세계, 별도 방문 루프, 26초 예열, 별도 실시간 시계가 없다.
- `card-worlds.js`는 하나의 WebGL 렌더러를 현재 보이는 카드 영역으로 나누어 그린다.
  각 카드가 `lab.runs`의 현재 world를 참조하므로 초기화 후에도 이전 세계를 보여주지 않는다.
  카드용 가구는 인스턴싱한 간소화 모델이며 지도 좌표와 진열안은 상세 보기와 같다.
  고객은 상세 보기와 같은 Blender 스킨·모션을 사용한다. 다른 페이지나 모바일에서 보이지 않는 카드는
  그리기만 생략하고 시뮬레이션 계산은 계속한다. 다시 보이면 현재 상태의 포즈를 그린다.
  카드의 상품 포장은 공유 진열 좌표마다 한 페이싱을 표현하며 재고 개수와 비례하지 않는다.
  실제 수량은 숫자로 표시하며, 재고가 0이면 해당 상품의 포장은 모두 숨긴다.
- 스켈레톤·걷기·팔 동작·회전·계산원 애니메이션은 해당 world의 실제 시간 변화량을 사용한다.
- 카드 지표와 이벤트 로그도 같은 world.snapshot()에서 읽는다. 물건을 담는 순간에는
  재고만 차감되고, 결제 동작이 완료된 실제 이벤트에서 매출이 증가한다.
- 렌더러 snapshot의 renderedTime / renderedRunId는 실제 그린 시점과 실행 ID다.
  UI의 syncProbe는 지표와 3D가 동일 실행·시간·좌표를 읽었는지 확인하기 위한 DOM 검사값이다.
  `cardViews.cards`는 각 카드가 실제로 그린 시각·실행 ID·고객 좌표를 기록한다.
  `visible:false`인 화면의 마지막 그린 값은 현재 엔진 값으로 위장하지 않는다.

배속은 요청한 속도다. 기기 성능이 부족하면 계산 예산으로 전체 세계를 함께 늦추고
실효 배속·밀린 시간을 표시한다. 숫자나 3D 중 하나만 앞서 진행하지 않는다.
일시 정지나 배속 변경 시 아직 계산하지 않은 벽시계 잔여 시간은 버려 지연된 고배속이
사용자 조작 후 계속 실행되지 않게 한다. 이미 계산된 사건이나 지표는 되돌리지 않는다.

고배속에서는 한 렌더 프레임 사이에 짧은 행동 여러 개가 끝날 수 있다.
이때 현재 상태만 화면에 보이지만 모든 행동은 엔진에서 순서대로 계산된다.
경과 시계는 실제 엔진 초 단위다. 기본 32배속의 산술상 24시간은 45분이다.
빠른 하루 비교용 900배속은 산술상 96초지만 실제 실행 시간은 연산·그리기 성능에 따라 더 길다.
UI에 표시되는 실효 배속이 기준이며, 32배속에서 몇 분 안에 하루가 끝난다고 보장하지 않는다.

## 하루의 경계와 재고 보충

실험 종료 조건은 재고 소진이나 방문 1,000회 완료가 아니라 **24:00 도달**이다.
잠재 고객 1,000명의 평가가 그 안에 모두 끝나도록 시드 기반 일정을 만든다.
기본 영업 시간은 24시간이며, 시간대별 가중치·입점 의향·가상 이벤트는 검증되지 않은 가정이다.
매출은 계산대에서 결제를 마친 순간에만 기록한다. 24:00까지 결제하지 못한 바구니는
창고로 반환하고 매출에 넣지 않으며, 일 종료 후 추가 결제나 보충도 발생하지 않는다.

상품별 `initialTotalStock = shelfStock + backroomStock`으로 시작한다.
매대 용량은 상품별 8~18개의 합성 설정이다. 보충 직원은 저재고 상품을 골라
창고 서비스 지점에서 매대 서비스 지점으로 A* 경로를 따라 이동한 다음 진열한다.
이동 속도 기본 1.1m/s, 상품별 서비스 시간 기본 6초, 보충 기준 기본 용량의 35%이다.
진열이 끝난 시점에 `min(창고 재고, 남은 매대 용량)`만 옮긴다.
창고까지 바닥나면 더 채울 수 없다. 외부 발주·배송·자동 재입고는 구현하지 않는다.
계산원과 보충 담당자는 별개인 **2개 역할 가정**이며, 실제 점주 1인의 업무 경합을 재현하지 않는다.
같은 SKU의 진열 재고는 매장 공용 수량으로 단순화했고, 보충 동작은 행사 매대 서비스 지점에서
표현한다. 각 선반 위치마다 별도 재고나 보충 직원-고객 간 충돌 회피까지 모델링하지는 않는다.

초기 수량 = 매대 + 창고 + 아직 결제하지 않은 바구니 + 결제된 수량이다.
매대 품절로 놓친 수요와 창고까지 소진된 품절을 구분한다.
상세 화면의 하루 분석은 시간대별 유입·입장·구매·매출과 보충 기록을 보여준다.
기록 JSON 내보내기는 이 실행의 전체 사건을 보존하며 실제 고객 데이터가 아니다.

## 계산과 가정

현재 상품은 직접 작성한 가상 SKU 24개, 앱의 페르소나는 공개 원본 합성 레코드 1,000개다.
`model.js`의 기존 수작업 5인 프로필은 레거시 테스트용으로 남겨둔다.
각 점포에 고유한 잠재 고객 1,000명의 일정을 만든다. 퇴장한 ID를 반복 경로에 재사용하지 않는다.
동시 고객 상한은 오피스·주거지 14명, 역세권 10명이다.

점포별 서로 다른 고정 시드·시간곡선·재고 배율을 사용한다. 원본 코호트는 같은 1,000개 레코드를
점포별로 결정적 순열·방문 시간으로 배정하며, 동일인의 실제 전국 이동을 추적하는 모델은 아니다.
같은 점포의 네 후보는 초기 총재고와 잠재 고객 일정·특성·입장 의향 난수를 공유한다.
후보가 만드는 혼잡·체류 시간 차이로 입장 거절이나 구매 결과는 달라질 수 있다.
A/B/C/D는 같은 1,000명에 대한 반사실 비교다. 카드별 사람 수를 독립 고객처럼 합하지 않는다.
신상품 탐색안(D)은 합성 신상품 5종을 2층에 모으고 함께 고를 상품을 3층에 둔 별도 4×6 배치다.
모든 후보는 같은 가상 상품 24개를 정확히 한 번씩 사용하며 신상품 후보에 별도 승률 보너스를 주지 않는다.

- **잠재 고객 평가**: 해당 시간까지 입장 또는 지나침이 확정된 사람 수.
- **입장 / 지나침**: 필요 없음·영업 종료·혼잡 등 입장 여부의 결과. 강제 대기 후 재시도하지 않는다.
- **방문 완료**: 출구에 도달한 고객 수. 결정 수, 입장 수, 결제 수와 다르다.
- **누적 모의 매출**: 결제가 완료된 상품 가격의 합계. 실제 POS 실적이나 미래 예측이 아니다.
- **누적 매출총이익**: 실제 결제액 - 판매된 상품의 합성 매입원가.
  원가율은 식사 71%, 음료 64%, 과자 62%, 건강식 68%를 10원 단위로 반올림한 가정이다.
  임대료·인건비·세금·프로모션 할인·폐기 비용은 포함하지 않는다.
- **미충족 구매 시도 비율**: 품절로 실패한 구매 시도 / 전체 구매 의향 발생 수.
  같은 사람이 다른 코너에서 다시 시도할 수도 있으므로 잃은 고객 수나 통계적 결품 확률이 아니다.
- **판단 수**: 실제 진열 앞에 도착한 뒤 인지한 상품을 비교한 횟수.
- **층별 지표**: 행사 매대에 실제 도착한 고객의 노출·인지·집기. 다른 코너의 선택은 포함하지 않는다.

행사 매대 층별 기본 인지 확률은 미검증 가정이며 정확한 좌표·주변 상품·혼잡·신상품·이벤트
맥락도 판단 입력에 포함된다. 로컬 기준선은 제한된 설명 가능한 규칙을 사용하고,
선택적 JEV 모드는 이 맥락·원문 페르소나를 실제 평가 요청에 전달한다.
이 위치 효과는 실제 상품 선택과 재고 차감에 반영되며 단순한 팔 동작 변경만이 아니다.
매대 재고 0인 상품은 바로 집을 수 없고 3D 상품 모델도 숨겨진다. 창고에 재고가 있으면
보충을 마친 뒤 다시 나타난다. 수량은 음수가 되지 않는다.

행사 매대뿐 아니라 다른 코너도 실제 배치의 SKU·정확한 좌표·주변 상품을 읽는다.
미방문 코너에 구매 가능한 상품이 있는지 평가하고 실제 상품 위치에 가까운 접근 지점까지
장애물을 피하는 경로를 찾는다. 코너 이름에 연결한 임의 상품 바구니나 모든 고객의 고정 순환 동선이 아니다.
단, 상품별 재고는 매장 공용 수량이며 위치별 별도 수량까지 분리하지 않는다.

`behavior.js`는 알려진 `생수만`·신상품 기피·가격 의식·제품 회피 기억 등 제한된 표현을 해석한다.
모르는 이야기와 식이 제약은 원문으로 남기며 완전한 자연어 이해나 알레르기 안전 필터라고 주장하지 않는다.
예산 이내라는 이유만으로 비싼 상품이 유리해지지 않도록 남은 예산 대비 가격 비중을 비용 항으로 반영한다.
잔액 1,700원 미만을 일괄 종료하는 규칙 대신 실제 구매 가능한 상품이 있는지 확인한다.
한 번 싫다고 기록한 알려진 상품의 회피와 이미 담은 상품 제한도 적용한다. 이 계수들은 실측으로 학습하지 않았다.

같은 초기 재고가 하루 안에 모두 팔리면 후보별 최종 매출이 같을 수 있다.
강제로 우승 후보나 상승률을 만들지 않는다. 시점별 매출·체류·품절 실패 등은 달라질 수 있다.
완료 전 진행 중 매출만으로 성과 순위를 확정하지 않는다. 승인 가능 시점도 24시간 종료 후다.

## 파일

- `lab.js`: 점포 설정, 기본 전체 9점포 × 4안 = 36개 실행, 단일 고정 간격 시계·배속·정지·진행 관리.
  명시적인 `everyday` / `destination` / `neighborhood` 그룹도 지원한다(각 3점포 × 4안).
- `world.js`: 고객 목적·공간 행동·상품 선택·재고·결제·이벤트
- `day.js`: 시간대별 잠재 고객 일정, 입장 의향 가정, 초기 매대·창고 분리
- `personas.js`, `data/nemotron-korea-sample.json`: 출처가 있는 합성 원본 1,000개와 방문별 사본
- `scripts/import-personas.mjs`, `data/PERSONA-SOURCE.md`: 리비전 검증 추출기·CC BY 4.0 출처·가정 설명
- `behavior.js`, `context.js`: 제한된 행동 제약·가격·기억 규칙과 실제 공간·상품 판단 입력
- `jev.js`, `server.mjs`: 실제 JEV 평가 API, 응답 검증·호출 한도·추적·비밀키의 서버 보관
- `store.js`: 선택한 world를 그리는 3D 렌더러, 카메라·고객 선택
- `card-worlds.js`: 보이는 카드의 공유 WebGL 렌더러와 점포별 3D 장면
- `components/ui/store-pager.js`, `store-pages.css`: 4점포 단위 페이지, 네이티브 터치 스크롤·스냅,
  마우스 드래그·버튼·점·방향키 이동과 데스크톱 16카드 / 모바일 점포별 2×2 배치.
  페이지 컨트롤은 시뮬레이션 상태를 소유하거나 초기화하지 않는다.
- `components/ui/simulation-card.js`, `simulation-cards.css`: 첨부된 course-card의
  공통 헤더·제목·진행 막대·다크 푸터를 모든 라이브 카드에 적용한 컴포넌트.
  기존 앱은 바닐라 JavaScript이므로 React/Tailwind로 이관하지 않았다. A/B/C/D는
  초록/주황/파랑/보라로 일관되게 구분한다. 하단 인물 표시는 고객·보충 역할 아이콘이며
  실제 인물 사진이 아니다. 16카드 요약에는 매출·진행률·시각 등을 표시하고 상세 지표는 확대 창에서 본다.
- `characters.js`: Blender 스킨 복제와 독립 AnimationMixer, 8종 모션
- `app.js`, `index.html`, `styles.css`: 다점포 비교 보드와 선택 실험 상세
- `maps.js`, `navigation.js`: 9개 합성 도면·공유 가구 경계와 A* 경로 탐색
- `model.js`: 공통 상품·페르소나·후보 데이터. 기존 별도 배치 함수는 회귀 검증용으로
  남아 있지만 현재 화면의 계산에는 사용하지 않는다.
- `lab.test.js`, `world-sync.test.js`: 단일 시계, 프레임 분할 재현성, 배속·정지,
  실제 결제 이벤트와 화면 원천 지표 일치 검증
- `card-sync.test.js`: 카드 뷰포트·스크롤·잘림 계산 검증
- `store-pager.test.js`: 4곳 / 4곳 / 1곳 분할과 페이지 이동·제스처 계약 검증
- `day.test.js`, `day-lab.test.js`: 하루 경계·유입 퍼널·유한 재고·보충·공유 시계 검증
- `personas.test.js`, `persona-integration.test.js`: 원문 무결성·고유성·근거·시간대·후보 간 UUID 일정 대응 검증
- `behavior.test.js`, `jev.test.js`, `jev-world.test.js`, `server.test.js`: 제한된 의미 규칙·모의 JEV 응답·시계 정지·실패 중단·서버 보안 검증; 외부 모델 호출 없음
- `renderer-owner.test.js`: 보충 직원의 엔진 상태·좌표·그린 시간 동기화 검증
- `VERIFICATION.md`: 실제 수행한 검증과 제한
- `assets/ASSET_NOTES.md`: Blender 에셋·생성 텍스처 출처와 한계

## 범위와 한계

기본은 시드 기반 로컬 규칙 시뮬레이션이며 JEV 실호출은 명시적으로 켜는 제한된 실험이다.
실제 GS 추천·Smallville식 기억 검색/반성은 없으며, 현재의 짧은 방문 기억·제한된 회피 규칙은 이를 대체하지 않는다.
3D 캐릭터는 19개 본과 8개 모션을 가진 원본 모델이지만 실사 스캔·전신 IK는 아니다.
매장 도면은 가상이며 카페 테이블은 정적 장애물이다.

현재 개선 범위는 **실제로 동기화된 36개 공간 시뮬레이션과 페이지당 최대 16개 화면 비교**다.
앞서 작성한 제출용 목표 전체(20개 상품, 지난주 데이터 분석, 실제 인증·서버 승인,
실제 수요로 보정한 24시간 예측, GitHub/Vercel 배포, 신상품 추천)를 완료했다고 주장하지 않는다.
구현된 승인 기능은 같은 브라우저의 localStorage에 저장하며 실제 접근 제어가 아니다.
