{
  "name": "gs2500-desktop-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "build:site": "node scripts/build-site.mjs --site-only",
    "dev": "npm run build && node scripts/serve-site.mjs",
    "preview": "node scripts/serve-site.mjs",
    "test": "npm run test:data && npm --prefix demo test",
    "test:data": "node --test test/*.test.js"
  }
}
